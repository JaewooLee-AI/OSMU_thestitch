# 더스티치 OSMU 워크벤치 — 구축 완료 보고

> **기록용 문서입니다.** 2026-09-07 Streamlit 버전 기준 보고이며, 이후 UI는 Flet 데스크톱 앱으로
> 옮겨졌고 Streamlit(`app.py`, `views/`)은 제거됐습니다. 현재 실행 방법과 구조는 README.md를 보세요.

> 이 문서는 작업 완료 시점의 최종 보고 내용입니다.
> IDE 한글 표시가 깨질 경우, 앱에서 **📋 구축 보고서** 페이지를 열면 브라우저에서 정상적으로 읽을 수 있습니다.
> (`streamlit run app.py` → 사이드바 맨 아래)

- **작업일**: 2026-09-07
- **위치**: `/Users/jwlee/project/OSMU_thestitch`
- **실행**: `streamlit run app.py`
- **규모**: 프로젝트 코드 5,033줄 / 42개 파일

---

## 1. 통합 결과

**OSMU_admin**(Streamlit + Supabase + 폴링 워커)과 **OSMU_web**(Next.js 워크벤치)을
**단일 Streamlit 앱 + 로컬 SQLite**로 통합했습니다.

3-tier 구조의 존재 이유는 Vercel 서버리스 함수의 **10초 타임아웃** 하나였습니다.
한 프로세스가 되니 우회할 대상 자체가 없어져, 아래가 전부 사라졌습니다.

| 사라진 것 | 이유 |
|---|---|
| Supabase Postgres | 생산자·소비자가 같은 프로세스인 큐는 순수 오버헤드 |
| `worker_runner.py` 폴링 루프 | 생성이 `st.status` 안에서 동기 실행 |
| Supabase Realtime 구독 | 같은 프로세스라 그냥 리턴값 |
| service_role 키 / RLS 정책 | 브라우저에 노출될 클라이언트가 없음 |
| 30일 TTL cron + Edge Function | 무료 티어 500MB/1GB 한도 우회 장치. 로컬 디스크엔 한도 없음 |

### ⚠️ 잃은 것 (명시)

OSMU_web은 Zustand로 **타이핑 즉시** 시뮬레이터가 다시 그려졌지만,
여기서는 Streamlit의 **rerun 왕복이 한 번** 있습니다.
시뮬레이터를 "타이핑하며 보는" 용도가 아니라 "완성된 초안을 발행 전 검증하는" 용도로 쓰는
실제 작업 흐름에서는 감수할 만한 교환이라 판단했습니다.

---

## 2. 사진 분석 토큰 최소화 (요청 사항)

기존에는 **사진 1장당 1회 호출**로, 1024px 이미지와 담당자 메모가 들어간 ~350토큰짜리 지시문을
매번 같이 보냈고, 같은 제품 컷을 열 번 써도 열 번 다시 분석했습니다.
다섯 개 층으로 줄였습니다 (영향이 큰 순서).

| # | 기법 | 효과 |
|---|---|---|
| 1 | 내용 해시(sha256) 캡션 캐시 | 재사용 시 **토큰 0** — 검증: 2회차 실행 API 호출 **0건** |
| 2 | 메모 분리 프롬프트 | 기존엔 "설명 + 이 글 어디에 배치"를 함께 물어 캡션이 글마다 달라져 **캐시 자체가 불가능**했음. 배치 판단은 어차피 메모·기사·전체 캡션을 다 가진 텍스트 호출로 이동 (더 싸고 역할 분담도 더 맞음) |
| 3 | 벤더별 최소 과금 해상도 | 아래 표 참조 |
| 4 | 배치 호출 (최대 6장/회) | 지시 프롬프트를 N번이 아니라 1번만 지불 |
| 5 | 출력 상한 40~45토큰/장 | `max_output_tokens` + 프롬프트에 글자 수 명시 |

### 벤더별 사진 1장 토큰 비용

| 벤더 | 절약(384px) | 균형(768px) | 고화질(1024px) | 축소 없이 3024×4032 |
|---|---:|---:|---:|---:|
| Gemini | **258** | 258 | 1,032 | 6,192 (**24배**) |
| OpenAI (`detail=low`) | **85** | 85 | 85 | 85 (해상도 무관 고정) |
| Anthropic | **196** | 786 | 1,398 | 16,257 (**83배**) |

추가로:
- 업로드 시점에 모든 사진을 **1280px JPEG로 정규화**해 저장하므로, 폰 원본 대비 실제 절감폭은 위 표보다 더 큼
- **가드레일 감사도 트윗 낱개 → 스레드 일괄**로 변경 (5개 스레드에서 4회 → 1회)
- ⚙️ 설정 → 사용량 탭에서 **프로바이더가 실제로 반환한 usage 값**으로 절감률 확인 가능
- 급하면 [이미지 분석 사용] 토글을 꺼서 비전 호출을 0으로 만들 수 있음

---

## 3. Brand Kit — 회사 자료에서 실측

`company_info/` PDF에서 추출해 **첫 실행 시 자동으로 채워집니다** (`core/brand_seed.py`).
빈 칸에서 시작하지 않으므로 **첫 초안부터** 제품명·가격대·인증·화법을 알고 씁니다.

- **핵심 팩트 15개** — 새활용제품인증 4건, 2023·2024 새활용산업 육성지원사업 선정, 기부 한복 200벌 등
- **용어집 14개** — 더봄봄 / 새활용 / 행복인형 / 향낭 / 짱리본핀 / 금빛 라이프 굿즈 …
- **SEO 키워드 10개** · **금기어 사전 27쌍** · **우수 포스팅 샘플 1개**
- **페르소나 & 톤앤매너** — "환경 이야기를 첫 문단에 꺼내지 않는다", "예뻐서 골랐는데 알고 보니 의미까지 있더라"는 순서
- **한복 오방색 팔레트** — 자주 `#A6224B` / 쪽빛 `#2B4C8C` / 금박 `#C9A227` / 옥색 `#86C1B3` / 한지 `#FBF7F2`
- **채널** — 블로그 `migoi1`, 인스타 `@thestitch_artplay`, `thestitchstore.co.kr` (sns_info.txt 기준)

브랜드 킷 데이터는 유사도 검색(RAG)이 아니라 **매번 전체 주입**합니다.
용어집 14개는 전부 넣어도 ~600토큰이고, 검색이 한 번 빗나가 '더봄봄'을 '더 봄봄'으로 쓴 초안이
나오는 쪽이 훨씬 비쌉니다.

### 중요한 판단: 의료법 → 그린워싱

`OSMU_admin`의 가드레일은 **의료법·표시광고법** 기준이었습니다 (원래 헬스케어 고객용).
더스티치는 새활용 제품과 환경 교육을 팝니다. 실제 법적 리스크는

- 「환경기술 및 환경산업 지원법」 **환경성 표시·광고 관리제도**
- 「표시·광고의 공정화에 관한 법률」

쪽이고, 전형적 위반은 "암을 고친다"가 아니라 **그린워싱**입니다 —
포괄적 환경성 주장("100% 친환경"), 근거 없는 정량 효과("탄소 3kg 절감"), 미보유 인증의 암시.
이 기준으로 가드레일을 다시 썼습니다 (`ai_workers/guardrail.py`).

---

## 4. 테스트 중 잡은 실제 결함 3건

**① 치환어가 비문을 만듦**
`"국내 최초로"` → `"국내에서도 보기 드문**로**"`.
한국어는 조사가 뒤에 붙으므로, 치환어를 원본과 **같은 품사·활용형**으로 재설계하고
(관형어→관형어, 명사→명사, 부사→부사), 짧은 항목이 긴 항목을 가리지 않도록
**길이 내림차순 매칭**으로 고쳤습니다.

```
입력: 저희 제품은 100% 친환경이며 국내 최초로 무독성 소재를 사용한 최고급 굿즈입니다.
출력: 저희 제품은 친환경적이며 국내에서도 보기 드물게 유해물질 저감 소재를 사용한 고급 굿즈입니다.
```

**② 같은 사진 중복 분석**
한 실행 안의 중복 참조는 캐시가 잡지 못해 두 번 분석됐습니다. (배치 `[6,3]` → `[6,2]`)

**③ paste worker HTML 미이스케이프**
본문의 `&`나 `<`가 네이버 에디터에서 마크업으로 해석되어 주변 텍스트를 삼킬 수 있었습니다.

---

## 5. 검증 내역

- 6개 페이지 전부 로드, **Python 예외 0건**
- 뉴스 검색 **실제 동작** — 한복 새활용 관련 기사 6건 수집 확인
- 4채널 시뮬레이터 렌더 확인 — 인스타 **125자 절단선**, X **3장 그리드**, 쇼츠 **데드존/SAFE AREA**
- 모의 LLM 전체 파이프라인 통과 (7단계, 4채널 산출물 전부 저장)
- 가드레일: 금기어 치환 + 미탐지 없음(정상 문장 무변경) 확인
- AES-256-GCM **위변조 탐지**(InvalidTag) 및 마스터키 **0600** 권한 확인
- 중복/오류/장애 응답 등 **실패 경로 4종** 모두 우아하게 degrade 확인

### ⚠️ 남은 사항 (미검증)

LLM API 키가 없어 **실제 모델 호출 경로는 모의(mock)로만 검증**했습니다.
⚙️ 설정 페이지에서 키를 등록하시면 [연결 테스트 · 저장] 버튼으로 실호출을 확인할 수 있습니다.

---

## 6. 시작하기

```bash
cd /Users/jwlee/project/OSMU_thestitch
source .venv/bin/activate          # 이미 생성해 둠 (의존성 설치 완료)
streamlit run app.py
```

1. 첫 실행 시 `data/osmu.db` 생성 + 브랜드 킷 자동 세팅
2. **⚙️ 설정 → LLM 벤더**에서 API 키 1개 이상 등록 → [기본 생성 모델] 지정
3. **✍️ 워크벤치**에서 [➕ 새 콘텐츠] → 메모·사진 입력 → [🪄 초안 생성]
4. 우측 시뮬레이터로 4채널 확인 → [🚀 네이버 게시 대기열로]
5. **🚀 네이버 게시**에서 로그인 세션 저장 후 [지금 게시]

네이버 게시 자동화를 쓰려면 최초 1회: `playwright install chromium`

---

## 7. 화면 구성

| 페이지 | 하는 일 |
|---|---|
| 📊 대시보드 | 상태별 칸반, 저장소 사용량, 누적 절약 토큰 |
| ✍️ 워크벤치 | 좌: 메모·사진·본문 편집 / 우: 4채널 시뮬레이터 |
| 📰 뉴스 큐레이션 | 브랜드 키워드로 Google/네이버 뉴스 검색 → 워크벤치 전달 |
| 🧵 브랜드 킷 | 페르소나·톤앤매너·핵심 팩트·용어집·SEO·금기어 사전 |
| 🚀 네이버 게시 | 로그인 세션 저장, 스마트에디터 반자동 입력 |
| ⚙️ 설정 · 토큰 | LLM 벤더 등록, 이미지 분석 토큰 정책, 사용량 리포트 |
| 📋 구축 보고서 | 이 문서 |

---

## 8. 데이터와 백업

전부 `data/` 안에 있습니다.

| 파일 | 내용 |
|---|---|
| `osmu.db` | SQLite (콘텐츠, 브랜드 킷, LLM 설정, 캡션 캐시, 사용량) |
| `assets/` | 업로드 사진 (1280px JPEG 정규화 저장) |
| `.master_key` | API 키 암호화 마스터 키 (0600, 최초 사용 시 자동 생성) |
| `naver_state.json` | 네이버 로그인 세션 쿠키 |

이 디렉터리만 백업하면 됩니다. `.gitignore`에 이미 제외되어 있습니다.

`data/`를 클라우드에 동기화한다면 마스터 키는 밖으로 빼세요:

```bash
python -c "import os,base64;print(base64.b64encode(os.urandom(32)).decode())"
# → .env 의 ENCRYPTION_MASTER_KEY_BASE64 에 넣고 data/.master_key 삭제
```
