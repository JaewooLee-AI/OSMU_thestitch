# 이 저장소를 다른 회사에 맞춰 커스터마이징하기

이 저장소는 현재 **주식회사 더스티치 / 한복 업사이클링 브랜드 '더봄봄'** 전용으로 완성되어
동작 중입니다. 이 문서는 **다른 회사가 이 저장소를 clone해서 자기 브랜드용으로 바꿀 때**,
Claude Code가 무엇을 어떻게 고치면 되는지 알려주는 작업지시서입니다.

> 사용자가 "이 폴더/자료로 브랜드 킷 채워줘", "회사 이름을 OO으로 바꿔줘", "우리 회사에 맞게
> 세팅해줘" 같은 요청을 하면 이 문서의 절차를 따르세요.

## 반드시 지킬 원칙

1. **더스티치용으로 이미 동작하는 로직 구조 자체는 바꾸지 않습니다.** 함수 시그니처, DB 스키마,
   파이프라인 순서, `flet_app/`·`ai_workers/`·`core/`의 아키텍처는 그대로 둡니다. 바꾸는 것은
   **아래 목록에 명시된 "값"들뿐**입니다 — 더스티치의 이름·페르소나·핵심 팩트·색상·법률 기준 같은
   데이터를, 새 회사의 같은 종류의 데이터로 교체하는 작업입니다.
2. **회사 자료에 없는 사실을 지어내지 않습니다.** `core_facts`·가격·인증 건수 등은 사용자가 준
   자료(PDF, 웹사이트, 대화 중 알려준 내용)에서만 뽑습니다. 불확실하면 사용자에게 되묻습니다.
3. **API 키(LLM/네이버)는 절대 파일에 하드코딩하지 않습니다.** 앱 실행 후 ⚙️ 설정 화면에서
   사용자가 직접 입력하도록 안내하세요 — 키는 DB에 암호화되어 저장되는 것이 이 앱의 설계입니다.
4. **`git commit`/`git push`는 사용자가 명시적으로 요청하기 전까지 하지 않습니다.**
5. 큰 폭으로 값을 바꾸기 전에 — 특히 3단계(컴플라이언스 기준)와 4단계(색상) — **바뀔 내용을
   요약해서 사용자에게 먼저 보여주고 확인받으세요.**

---

## 0단계 — 회사 자료 확보

새 회사의 소개 자료(회사소개서 PDF, 웹사이트, SNS 정보, 로고 이미지, 판매 상품/서비스 목록 등)가
필요합니다. `company_info/` 폴더가 이미 있다면 그 자리에 **새 회사 자료로 교체**하도록 안내하고,
없다면 사용자에게 자료 위치를 묻거나 대화로 직접 받으세요. 로고 이미지가 있다면 Read 도구로 직접
열어 주요 색상을 눈으로 확인할 수 있습니다(멀티모달).

---

## 1단계 — 브랜드 정체성 · 목소리 · SEO (필수)

모든 값이 `core/brand_seed.py` 한 파일에 모여 있습니다. 이 파일의 구조를 그대로 유지한 채
**값만** 새 회사 것으로 교체하세요.

| 상수 | 무엇을 넣나 |
|---|---|
| `BRAND_NAME`, `SUB_BRAND` | 회사 법인명, (있다면) 브랜드/제품 라인명 |
| `HOMEPAGE`, `NAVER_BLOG_ID`, `INSTAGRAM_HANDLE` | 실제 채널 주소/아이디 (네이버 블로그 아이디는 `blog.naver.com/<이 값>`) |
| `INDUSTRY` | 업종을 한 문장으로 |
| `PERSONA` | 글을 쓰는 화자의 인격 — 경력, 말투의 태도, 무엇을 앞세우고 무엇을 말하지 않는지 |
| `TONE_AND_MANNER` | 문장 규칙 (존댓말 여부, 길이, 금지 표현, 이모지 사용 등) — 불릿 형식 유지. 길이는 "2~3줄"처럼 느슨하게 쓰지 말고 "한 문장 40자 안팎"처럼 숫자로 쓰세요 |
| `CORE_FACTS` | 검증 가능한 회사 사실 10~15개 (인증, 연혁, 실적, 가격대 등). **이 목록의 충실도가 SEO·컴플라이언스 판정 품질을 직접 좌우합니다** — 부실하면 이후 단계 품질이 전부 무너집니다 |
| `TERMINOLOGY` | 회사 고유명사·제품명 용어집 (표기 통일용) |
| `SEO_KEYWORDS` | 네이버 검색 노출을 노릴 키워드 8~15개 — 브랜드 고유명사가 아니라 **실제로 검색되는 일반 명사** 위주 |
| `BLACKLIST_MAP` | 아래 3단계와 함께 결정 |
| `FEW_SHOT_SAMPLES` | 원하는 문체의 실제 예시 글 1~2편 (있다면). 모델은 이 글의 **말투와 문장 길이**를 따라 하므로, 문장이 길고 딱딱한 보도자료식 글을 넣으면 톤앤매너에 "짧게"라고 써도 장황해집니다 |
| `SEED_FIXES` | 새 회사는 **빈 리스트 `[]`로** 두세요. 이전 버전이 이미 저장해 둔 브랜드 킷 문구를 한 번만 고치는 용도라, 새로 세팅하는 회사에는 해당 사항이 없습니다 |

다 채운 뒤 `seed_if_empty()` 함수 자체는 건드리지 마세요 — 이름 그대로 **비어 있을 때만** 실행되는
함수라, 그대로 둬도 새 값으로 정상 동작합니다.

**데이터 폴더 위치:** 저장소 루트에 `data/`가 있으면 그곳을 쓰고, 없으면 사용자별 폴더
(Windows `%APPDATA%\OSMU_THESTITCH`, macOS `~/Library/Application Support/OSMU_THESTITCH`)를
씁니다. `.env`의 `OSMU_DATA_DIR`로 직접 지정할 수도 있습니다 (`core/db.py`의 `_default_data_dir`).
아래에서 "`data/` 폴더"는 이 중 실제로 쓰이는 폴더를 뜻합니다 — `data/`만 지우고 사용자별 폴더에
예전 데이터가 남아 있으면 그쪽이 그대로 쓰이니 둘 다 확인하세요.

**중요 — 이미 실행해서 `data/` 폴더가 생겼다면:** `seed_if_empty()`는 `brand_kit`이 비어 있을 때만
동작하므로, 이미 한 번 실행된 앱(더스티치 데이터로 이미 채워진 상태)에는 파일을 고쳐도 반영되지
않습니다. 이 경우 둘 중 하나를 선택하세요:
- (권장, 처음 세팅이라면) `data/` 폴더를 통째로 삭제하고 앱을 다시 시작 — `.gitignore`에 이미
  제외되어 있어 삭제해도 안전합니다.
- (이미 다른 작업 데이터가 있어 지우면 안 된다면) 아래처럼 직접 반영:
  ```bash
  python3 -c "
  from core import repo
  repo.save_brand_kit(
      brand_name='...', sub_brand='...', ...  # brand_seed.py와 동일한 키
  )
  "
  ```

---

## 2단계 — 컴플라이언스 기준 (필수 검토)

`ai_workers/guardrail.py`의 `AUDIT_SYSTEM_PROMPT`(파일 상단)는 **더스티치의 실제 법적 리스크인
환경성 표시·광고(그린워싱)** 기준으로 고정 작성되어 있습니다. 다음을 확인하세요.

- **새 회사도 환경·새활용 관련 주장을 하는 브랜드라면** → 이 프롬프트는 거의 그대로 써도 됩니다.
  회사명·업종 설명 문구 정도만 자연스럽게 맞추세요.
- **다른 업종(화장품, 식품, 의료·건강기능식품, 일반 소비재 등)이라면** → 이 프롬프트가 지금
  전혀 맞지 않는 법 조항을 검토 기준으로 삼고 있는 것입니다. 해당 업종에 실제로 적용되는 표시·광고
  규제(예: 화장품법, 식품 등의 표시·광고에 관한 법률, 의료기기법, 또는 일반 표시·광고의 공정화에
  관한 법률)로 **프롬프트 내용을 다시 작성**하세요. 단, 다시 쓰는 것은 **검토 기준(무엇을
  찾아낼지) 부분만**입니다. 프롬프트 뒷부분의 출력 형식 — `issues` 항목마다 `phrase`·`note`·
  `replacement`를 받는 규칙과 JSON 예시 — 는 코드(`run_llm_audit`)가 그대로 파싱하므로 바꾸지 마세요.
  감사는 글 전체를 다시 쓰지 않고 `phrase → replacement` 교정만 돌려주며, 형식이 깨지면
  '검수 미완료'로 처리됩니다. 이건 법률 문구이므로:
  - 무엇을 왜 바꿨는지 사용자에게 요약해서 보여주고,
  - **"실제 서비스 전 법무 검토를 권장한다"는 점을 반드시 사용자에게 알리세요.**
- `core/brand_seed.py`의 `BLACKLIST_MAP`(금기어 → 치환어 사전)도 같은 이유로 업종에 맞게
  다시 구성해야 합니다. 더스티치 목록은 "100% 친환경", "무독성" 같은 환경 주장 위주입니다.

가드레일을 아예 끄고 싶다면 코드를 건드릴 필요 없이 브랜드 킷 화면의 토글로 끌 수 있다는 점도
안내하세요 (`guardrail_enabled` 필드, 기본값 켜짐).

---

## 3단계 — 브랜드 컬러 (필수)

색상은 `core/brand_seed.py`의 `BRAND_COLORS` 딕셔너리 한 곳에만 있습니다 (키:
primary/primary_dark/secondary/accent/mint/bg/text/text_muted). Flet 테마(`flet_app/theme.py`)가
이 값을 그대로 가져다 쓰므로 여기만 바꾸면 됩니다.

새 회사의 로고나 브랜드 가이드에서 주요 색상 2~4개를 뽑아 제안하고, 사용자 확인 후 반영하세요.
로고 이미지가 있으면 Read 도구로 직접 보고 대표 색상을 눈짐작으로 골라도 됩니다.

---

## 4단계 — 선택 사항 (필요할 때만)

아래는 기능에는 영향 없는 화면 문구/기본값입니다. 새 회사가 한복·새활용 업종이 아니라면
어색하게 보일 수 있어 다듬으면 좋지만, 급하지 않으면 건너뛰어도 앱은 정상 동작합니다.

| 파일 | 내용 |
|---|---|
| `flet_app/main.py`의 `page.title`, `flet_app/pyproject.toml`의 `description`·`product`·`org`·`company` | 창 제목과 빌드된 exe의 제품명입니다. 창 제목은 실행 후 브랜드 킷의 `sub_brand`로 바뀌지만, 처음 뜨는 순간과 exe 이름에는 이 값이 보입니다. |
| `flet_app/simulators/__init__.py`, `instagram.py` | 시뮬레이터의 `"더봄봄"`/`"thestitch_artplay"` 기본값. 브랜드 킷 값이 비어 있을 때만 쓰이는 fallback이라 우선순위는 낮습니다. |
| `flet_app/views/workbench_view.py`, `brand_kit_view.py` 등 | 도움말·placeholder 예시 문구에 "리본핀" 같은 더스티치 예시가 섞여 있습니다. |
| `ai_workers/factsheet.py` | 공지/제품 정보 입력 필드(일시·장소·최소주문수량 등)는 소규모 제작·리테일 업종에 맞춰져 있습니다. 업종이 크게 다르면(예: 요식업, SaaS) 필드 구성 자체를 조정할 수 있습니다. |
| `ai_workers/keyword_curator.py`의 `TOPIC_LIMIT`(기본 2), `MAX_DOCS_PER_SEARCH`(기본 100) / `ai_workers/keyword_research.py`의 `DEFAULT_MAX_VOLUME`(기본 30,000) | 회사의 실제 사업 축 개수, 블로그 권위 수준에 따라 조정. |
| `README.md` | 현재 더스티치 기준 설명입니다. 최종적으로는 새 회사 소개로 갱신하는 것을 권장하되, 온보딩 자체에는 영향 없습니다. |

---

## 5단계 — 확인

1. `data/` 폴더가 없는 상태(또는 방금 삭제한 상태)에서 `python flet_app/main.py` 실행
2. 🧵 브랜드 킷 화면에서 새 값이 제대로 보이는지 확인
3. ⚙️ 설정 → LLM 벤더 / 네이버 API — **여기는 Claude Code가 대신 채우지 말고, 사용자가 직접
   화면에서 키를 입력**하도록 안내 (키는 암호화되어 DB에 저장됨)
4. 워크벤치에서 테스트 글 하나를 생성해, 브랜드 목소리·컴플라이언스 판정이 새 회사 기준으로
   그럴듯하게 나오는지 확인

---

## 참고 — 이 구조가 이렇게 된 이유

`core/brand_seed.py`는 원래부터 "회사마다 다시 쓰는 파일"로 설계되어 있습니다
(파일 상단 주석 참고: `seed_if_empty()`는 최초 1회만 실행되고, 이후에는 브랜드 킷 화면이
진실의 원천이 되어 관리자가 편집해도 이 파일이 덮어쓰지 않습니다). 이 문서의 절차는 그
설계를 그대로 따르는 것이지, 새로운 메커니즘을 추가하는 것이 아닙니다.
