# Core app
streamlit>=1.38
pandas>=2.0
Pillow>=10.0
python-dotenv>=1.0
cryptography>=43.0

# LLM providers (install only the ones you register in ⚙️ 설정)
openai>=1.40
anthropic>=0.34
google-genai>=1.0

# News curation
requests>=2.32
beautifulsoup4>=4.12
feedparser>=6.0

# Naver semi-automatic publish (needs `playwright install chromium` once)
playwright>=1.46
pyperclip>=1.9
# Windows-only: writes the real HTML clipboard format (CF_HTML) so pasted
# content renders as formatted paragraphs instead of literal <p> tags —
# without it, core/clipboard_utils.py silently falls back to pyperclip's
# plain-text copy on any Windows machine that's missing this package.
pywin32; platform_system == "Windows"

# NOTE: no `supabase` — the whole data layer is local SQLite (core/db.py).
